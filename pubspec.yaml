name: speleo_app
description: App gestione gruppo speleo
publish_to: 'none'

environment:
  sdk: ^3.10.0-290.4.beta

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  url_launcher: ^6.2.5
  firebase_storage: ^12.1.2
  image_picker: ^1.0.7
  pdf: ^3.10.7
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/gsu_logo.png
